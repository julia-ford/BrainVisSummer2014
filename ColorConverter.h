#ifndef colorconverter_h
#define colorconverter_h
#include "rgba.h"

namespace ColorConverter{

	/// This is the only function named in the ColorConverter header file.
	/// I don't know what "Idx" is.
	int getIdxByBoysColor(const rgba& color);
};

#endif